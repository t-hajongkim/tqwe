# 실습 안내

논문 한 편이 **내 환자 데이터에 정말 쓸 만한지**를, GitHub이 단계마다 확인해 주는 과정으로
직접 돌려 봅니다. 코드를 쓰지 않습니다. 버튼을 누르고, 읽고, 승인하면 됩니다.

---

## 이 실습에서 만드는 것

AI에게 "이 논문 분석해 줘"라고 한 번 물어보는 것과, 아래처럼 **단계마다 멈춰서 확인받는 것**의
차이를 직접 보게 됩니다.

| 단계 | 무슨 일이 일어나나 | 내가 하는 일 |
|---|---|---|
| 1. 수집 | 최신 논문을 찾아 우리 데이터와의 관련성을 적어 둔다 | 실행 버튼 누르기 |
| 2. 기획 | 한 논문을 골라 "이렇게 분석하고 이렇게 보여드리겠습니다"를 제안한다 | 읽고 승인 |
| 3. 분석 | 승인된 계획대로 실제 데이터를 조회해 분석한다 | 결과 확인 |
| 4. 검증 | **다른 AI**가 숫자를 전부 다시 계산하고 출처를 다시 확인한다 | 지적사항 읽기 |
| 5. 정리 | 통과한 분석만 대시보드 한 페이지에 쌓인다 | 열어보기 |

4단계가 핵심입니다. 실제로 이 검증 AI가 분석 AI의 숫자 하나를 잡아냈습니다
(이웃 순도를 6.0%로 적었지만 다시 계산하니 5.0%였습니다).

---

## 미리 알아두실 것

- **AI가 한 번 일하는 데 10~20분 걸립니다.** 버튼을 누르고 화면이 멈춘 것처럼 보여도 정상입니다.
  기다리는 동안 다음 설명을 들으시면 됩니다.
- **환자 데이터는 가짜입니다.** 흉부 X선 272건, 환자 153명의 합성 데이터입니다.
- **AI는 환자 한 명의 값을 볼 수 없습니다.** 마스킹된 뷰를 통해 "몇 명, 몇 퍼센트" 같은
  집계만 조회할 수 있게 막아 두었습니다.

---

## 준비 (약 6분)

### 1. 내 저장소 만들기

**https://github.com/hy2219/medical-sdlc** 로 들어가서 초록색 **Use this template** →
**Create a new repository** 를 누릅니다.

- Repository name: 아무 이름 (예: `medical-sdlc-실습`)
- **Public** 선택 (마지막 실습의 대시보드 주소가 이때만 만들어집니다. 데이터는 전부
  합성이라 실제 환자 정보는 들어 있지 않습니다.)
- **Create repository** 클릭

### 2. AI가 풀 리퀘스트를 만들 수 있게 허용

내 저장소에서 **Settings** → 왼쪽 **Actions** → **General** →
맨 아래 *Workflow permissions* 에서

- **Allow GitHub Actions to create and approve pull requests** 체크
- **Save** 클릭

> 이걸 빠뜨리면 AI가 일은 다 해놓고 결과를 올리지 못합니다. 가장 흔한 실수입니다.

### 3. 데이터베이스 비밀번호 등록

**Settings** → 왼쪽 **Secrets and variables** → **Actions** →
**New repository secret**

- Name: `LLM_DB_PASSWORD`
- Secret: 아무 문자열 (예: `hands-on-2026`)
- **Add secret** 클릭

### 4. 대시보드 주소 켜기

**Settings** → 왼쪽 **Pages** → *Source* 를 **GitHub Actions** 로 바꿉니다.

### 5. Actions 탭 열어보기

위쪽 **Actions** 탭을 누릅니다. 워크플로 6개가 보이면 준비 끝입니다.
"I understand my workflows, go ahead and enable them" 버튼이 보이면 눌러 주세요.

---

## 실습 1 — 논문 수집하기 (약 15분)

### 하는 일

**Actions** 탭 → 왼쪽 목록에서 **Daily paper recommendations** 클릭 →
오른쪽 **Run workflow** 버튼 → 날짜 칸에 `2026-08-19` 입력 → **Run workflow**

### 무슨 일이 일어나나

AI가 먼저 우리 환자 데이터를 조회해서 "우리는 흉부 X선 272건이 있고 결절이 12건이구나"를
파악합니다. 그 다음 **그것을 근거로 검색어를 직접 만들어서** Nature 계열 논문을 찾습니다.
관련 없는 논문은 스스로 버립니다.

### 결과 확인

10~15분 뒤 저장소 첫 화면에 `recommended/2026-08-19/` 폴더가 생깁니다.

- `README.md` — 이 논문들이 **왜 우리 데이터와 관련 있는지** AI가 쓴 설명
- 논문마다 폴더 하나 (`paper.json`)

> 수집 결과는 자동으로 병합됩니다. 검토 대상이 아니라 기록이기 때문입니다.

---

## 실습 2 — 분석 요청하기 (약 3분)

### 하는 일

1. `recommended/2026-08-19/` 안에서 마음에 드는 논문 폴더를 하나 열고,
   `paper.json` 의 경로를 복사합니다.
   (예: `recommended/2026-08-19/cbec-retrieval-chest-xray-reliability/paper.json`)
2. 위쪽 **Issues** 탭 → **New issue** → **논문 분석 요청** 의 **Get started**
3. 세 칸을 채웁니다.
   - **논문 경로**: 방금 복사한 경로
   - **무엇을 중점적으로 봐주면 좋을까요?**: 여기가 제일 중요합니다.
     평소 궁금하던 것을 그대로 적으세요.
     > 예: "우리처럼 결절 증례가 적은 코호트에서도 이 방법이 통할까요?
     > 특히 놓친 결절(거짓음성)이 걱정입니다."
   - **참고할 내용**: 비워두셔도 됩니다.
4. **Submit new issue** ← 이슈를 등록하는 순간 AI가 시작합니다.

---

## 실습 3 — 제안 읽고 승인하기 (약 15분 대기)

### 무슨 일이 일어나나

AI가 논문 초록을 읽고, 우리 데이터베이스를 실제로 조회하고, 둘을 비교한 뒤
**"이렇게 분석해서 이런 그래프로 보여드리겠습니다"** 를 제안하는 풀 리퀘스트를 엽니다.

아직 분석은 하지 않습니다. 기획서만 씁니다.

### 하는 일

**Pull requests** 탭 → `Probing: ...` 제목의 풀 리퀘스트를 엽니다.

**Conversation** 화면 본문에 이런 내용이 있습니다.

- **논문이 쓴 데이터** — 규모, 라벨, 촬영 방식
- **우리 데이터** — 실제 조회한 숫자
- **차이점 표** — 무엇이 있고 무엇이 없는지
- **얻을 수 있는 것 / 얻을 수 없는 것**

읽어 보시고,

- 방향이 맞으면 → **Merge pull request** (= 승인)
- 다른 걸 보고 싶으면 → 아래 댓글로 적으세요. 다음 단계에 반영됩니다.

> **여기가 이 실습의 핵심입니다.** AI에게 한 번에 다 시키면 이 확인 단계가 없습니다.
> 분석이 끝난 뒤에 "그게 아니라 다른 걸 보고 싶었는데" 를 발견하게 됩니다.

---

## 실습 4 — 분석과 검증 결과 보기 (약 30분 대기)

승인하면 자동으로 두 가지가 연달아 일어납니다.

### 먼저, 분석 AI가 일합니다

승인된 계획대로 데이터베이스를 조회해서 분석하고, 그래프를 그리고,
`Analysis: ...` 풀 리퀘스트를 엽니다.

### 그 다음, **다른 AI**가 검사합니다

- **출처** — 논문 DOI, 저자, 저널, 초록의 문장을 원문에서 다시 확인
- **숫자** — 데이터베이스 쿼리를 **직접 새로 짜서** 다시 계산하고 대조
- **이전 AI** — 승인된 계획대로 했는지, 몰래 범위를 늘리지 않았는지

### 하는 일

`Analysis: ...` 풀 리퀘스트를 열고 **Files changed** 옆 리뷰 내용을 봅니다.

검증 AI가 이렇게 적어 둡니다.

```
검증 결과: 수정 요청
- [ ] 출처: 실패 — 논문 초록에 없는 내용을 단정했습니다.
- [ ] 데이터 분석: 실패 — k=10을 6.0%로 보고했으나 재계산 결과 5.0%입니다.
- [ ] 이전 에이전트: 실패 — 그래프의 x축 간격이 실제 간격을 왜곡합니다.
```

문제가 있으면 그대로 두고 다시 요청하시면 되고, 괜찮으면 **Merge pull request**.

---

## 실습 5 — 대시보드 열기 (약 2분)

병합하면 대시보드가 자동으로 다시 만들어집니다.

**Settings → Pages** 맨 위에 적힌 주소를 클릭하면 열립니다.
`https://<본인아이디>.github.io/<저장소이름>/` 형태입니다.

- 병합할 때마다 자동으로 갱신됩니다. 1~2분 정도 걸립니다.
- 왼쪽: 날짜 / 가운데: 그날 논문 / 오른쪽: 클릭한 논문의 분석
- 오른쪽 화면은 네 칸입니다. **무엇을 했나 → 무엇이 필요했나 →
  그것이 우리에게 있나 → 그래서 무엇이 남나**
- 맨 아래에 검증 AI의 판정이 그대로 붙어 있습니다.

---

## 잘 안 될 때

| 증상 | 원인과 해결 |
|---|---|
| 풀 리퀘스트 대신 이슈가 생겼다 | 준비 2번을 빠뜨렸습니다. 설정하고 다시 실행하세요. |
| 워크플로가 빨간 X로 끝났다 | 대개 준비 3번(비밀번호)입니다. 등록하고 다시 실행하세요. |
| 대시보드 주소가 404다 | 준비 4번을 빠뜨렸거나 아직 배포 중입니다. Actions 탭에서 Build dashboard 가 끝났는지 보세요. |
| 이슈를 올렸는데 아무 일도 없다 | 저장소 위쪽 **Actions** 탭을 열어 실행 중인 항목이 있는지 확인하세요. 시작까지 1분 정도 걸립니다. |
| 10분째 노란 점이 돌고 있다 | 정상입니다. AI가 논문을 읽고 데이터를 조회하는 중입니다. |
| 논문이 하나도 안 나왔다 | 그날 조건에 맞는 논문이 없었던 것입니다. 다른 날짜로 다시 실행하세요. |

---

## 오늘 가져가실 것

- AI에게 **한 번에 다 시키지 않고** 단계로 쪼개면, 매 단계에서 방향을 바로잡을 수 있습니다.
- **승인이 곧 기록입니다.** 누가 언제 무엇을 승인했는지 전부 남습니다.
- **AI를 AI가 검사하게** 할 수 있습니다. 그리고 실제로 오류를 잡습니다.
- 이 구조는 논문 리뷰만의 것이 아닙니다. 확인이 필요한 반복 업무라면 같은 틀을 씁니다.
